#[path = "../oracle_record.rs"]
mod oracle_record;
#[path = "../oracle_scan.rs"]
mod oracle_scan;

fn main() {
    eprintln!("mpla-poc-oracle is not implemented at the M1 checkpoint");
    std::process::exit(2);
}
