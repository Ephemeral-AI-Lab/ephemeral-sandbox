//! Static first-party plugin provider operation contracts.
//!
//! Runtime implementation lives in the daemon-side `plugin` crate; this module
//! only owns the typed request/response contracts consumed by operation parsing.

pub mod contract;
