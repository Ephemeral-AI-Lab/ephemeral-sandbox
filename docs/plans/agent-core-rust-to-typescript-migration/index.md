# EOS Agent Core Rust to TypeScript Migration

Status: Proposed
Date: 2026-06-10
Owner: eos-agent-core
Migration direction: Rust -> TypeScript
Project path: `/Users/yifanxu/machine_learning/LoVC/EphemeralOS/eos-agent-core`

This directory is the coordination point for specs that migrate the current Rust
`agent-core/` implementation into the TypeScript project at
`/Users/yifanxu/machine_learning/LoVC/EphemeralOS/eos-agent-core`.

The declaration here is intentionally narrow:

- It creates a home for migration specs.
- It declares `eos-agent-core/` as the TypeScript project path.
- It declares TypeScript as the target implementation language.
- It records the initial recommended third-party stack for the TypeScript
  target.
- It does not change the live Rust implementation, Cargo workspace, tests, or
  runtime behavior.

## Recommended Target Stack

| Concern | Recommendation | Adoption rule |
| --- | --- | --- |
| Workspace layout | `pnpm` workspaces | Baseline for TypeScript packages under the future migration layout. |
| Runtime and language | Node.js TypeScript with strict compiler settings | Baseline for new TypeScript implementation phases. |
| Durable state | `better-sqlite3` + `Kysely` | Baseline for local SQL persistence, migrations, and typed query access. |
| Runtime validation | Zod 4 | Baseline for request DTOs, provider payloads, tool inputs, persisted JSON, and compatibility checks. |
| Test runner | Vitest | Baseline for TypeScript unit and integration tests; authoring rules in Test Authoring Baseline below. |
| Observability | OpenTelemetry JavaScript packages | Baseline for run, attempt, provider, tool, and sandbox-host spans/events. |
| Structured logs | `pino` or the repo-selected structured logging wrapper | Phase-gated until the TypeScript runtime package is introduced. |
| Bounded concurrency | `p-queue` or a small crate-local limiter | Optional; use only for explicit request admission, provider, tool, or agent-run concurrency limits. |

Runtime primitives such as `AbortController` and `AsyncLocalStorage` should be
treated as first-class lifecycle tools even though they are not third-party
libraries. They should back cancellation trees, per-request context, and active
run supervision.

Do not introduce BullMQ, Temporal, Redux, Zustand, XState, Prisma, Drizzle, or
Redis as Phase 00 defaults. They require a later phase spec with a concrete
distributed-worker, UI-state, workflow-orchestration, or database-backend reason.

## Test Authoring Baseline

Applies to every Vitest suite under `eos-agent-core/packages/*`:

- One `describe` block per unit under test, with `it` titles written as
  behavior sentences (for example "retries only before visible output").
  Use `it.each` with templated titles for case tables.
- Use the optional `expect(value, message)` second argument when a single test
  asserts multiple scenarios, repeated counters, or timing bounds, so a failure
  names the step that broke. Single-scenario assertions rely on Vitest's
  default diff output.
- Vitest's annotation API (`context.annotate`) stays unused until a reporter
  that surfaces annotations is configured.

## Spec Index

| Spec | Status | Boundary | Verification |
| --- | --- | --- | --- |
| `phase-00-migration-declaration_SPEC.md` | Proposed | Planning declaration only; no live implementation boundary changes. | Spec records the migration identity, target stack, non-goals, and initial acceptance criteria. |
| `phase-01-project-setup_SPEC.md` | Completed | Setup-only TypeScript metadata under `eos-agent-core/`; Rust `agent-core/` remains behaviorally unchanged. | `CI=true pnpm install --frozen-lockfile`, `pnpm list --depth 0`, `pnpm exec tsc --showConfig`, `pnpm exec vitest --version`, and repo `git diff --check` hygiene. |
| `phase-02-llm-client_SPEC.md` | Completed | Additive TypeScript implementation: `@eos/contracts` minimum message DTOs and `@eos/llm-client` with two SDK-backed clients (Anthropic Messages via `@anthropic-ai/sdk`, OpenAI Responses via `openai`) behind one normalized event union and retry gate; Rust `agent-core/` remains live and unchanged. | `pnpm run check` in `eos-agent-core/`, including golden decode tests replaying both providers' copied SSE fixtures through injected-fetch SDK clients; `git diff --stat -- agent-core` stays empty. |
| `phase-02.5-provider-composition_SPEC.md` | Completed | Restructure within `eos-agent-core/packages/llm-client` only: recompose the two vendor-named clients into wire codecs (`wires/`) x credential schemes (`access/`) behind one generic `LlmStreamClient` and a `profiles.ts` registry (`createLlmClient`); adds `claude_coding_plan`/`codex_coding_plan` profiles and a live e2e harness (`pnpm run test:e2e`) whose codex suite auto-loads `~/.codex/auth.json`; copilot access and openai-chat wire are named seams; Rust `agent-core/` remains live and unchanged. | `pnpm run check` (unit, no network; 105 llm-client tests incl. the shared client-contract kit with golden `exact` bindings) green with all Phase 02 assertions surviving as moves; `pnpm run test:e2e` live codex battery (6 contract scenarios) green on this machine and clean-skip with `CODEX_AUTH_PATH=/nonexistent`; `git diff --stat -- agent-core` stays empty. |
| `phase-03-agent-loop-engine_SPEC.md` | Completed | Additive TypeScript implementation: `@eos/engine` agent-loop spine (thin while loop, dual transcript, event stream, interrupt/steer, non-tool-use termination); Rust `agent-core/` remains live and unchanged. | `pnpm run check` in `eos-agent-core/` (122 tests), including the scripted `MockLlmClient` loop suite (termination, steering, interrupt, tool batches); `git diff --stat -- agent-core` stays empty. |
| `phase-04-tool-framework_SPEC.md` | Completed | Additive `@eos/tool` package (flat Zod tool contract with fail-closed `defineTool` defaults, per-call pipeline, relocated batch executor with terminal-solo policy, external-only hooks with command + callback adapters, `AGENT_TOOLSET` assembly, and the two service-free families — submission and background — with per-family service injection; sandbox/agent/workflow families deferred with their services per decision 21) plus an engine restructure (`tools.ts`/`tool-runner.ts` removed behind one injected `ToolExecutor` port; generic `NotificationInbox` + `BackgroundSessionSupervisor` with spawn-site background-session handles, dispose-on-finish latch, and auto-wait; batch-result normalization; terminal-only exit with `submission` on the outcome — bare text never completes a run); contracts gain `AgentKind`, `AgentRunId`, `SandboxId`, `ToolCallResult`; testkit gains transcript fixtures and scripted tool/background-session-handle helpers; Rust `agent-core/` remains live and unchanged. | `pnpm run check` in `eos-agent-core/` (223 tests) including the Phase 04 §15 suite (pipeline order, relocated batch-runner suite + terminal-solo, hook exit-code protocol with real spawned scripts, supervisor lifecycle + cancel race + dispose latch, auto-wait woken by settlement and steer, isolated-mode batch snapshot, executor assembly per kind, engine normalization, single serialization point); ported Phase 03 loop suite green under a scripted executor; `git diff --stat -- agent-core` stays empty. |
| `phase-04.5-agent-runtime_SPEC.md` | Completed | `@eos/agent-runtime` composition root: `agent_name`-driven `startRun` for main/subagent/advisor runs with ordered `initialMessages`, startup-loaded `AgentProfileRegistry` + Markdown profile loader (`llm_client_id`, `max_turns`, `agent_kind`, `allowed_tools`, `terminal_tool`, body system prompt; all static validation at load), `LlmClientRegistry` from `.eos-agents/llm_clients.json` (codex CLI auth-file clients, `model_id`, `reasoning_effort`), per-run inbox/supervisor pairs, run registry with parent links, agent tool family (`run_subagent`/`ask_advisor`/`read_agent_run_transcript`) as `startRun` recursion, per-run JSONL transcript writer + byte-offset reader (the runtime is the event stream's single consumer), hook config loading, and the §8 disposal cascade (`caller_disposed`/`model_cancelled`); plus the §11 owned changes: `@eos/tool` binds profile-selected definitions (kind-keyed `AGENT_TOOLSET` deleted), the executor loses its inbox and hook `additionalContext` rides `metadata.hook_contexts` published by the engine loop, and `AgentRunState` gains `agent_name`; server transport/event broadcaster stays deferred; Rust `agent-core/` remains live and unchanged. | `pnpm run check` in `eos-agent-core/` (282 tests) including the Phase 04.5 §13 integration suite (profile loader/registry validation, llm client registry + codex auth-file validation, §4 wiring smoke with profile-selected tool exposure + notification drain, submission end-to-end, subagent round-trip with auto-wait + settlement notification + transcript read, advisor ask + caller-abort cancellation, disposal cascade recording `caller_disposed` and model-initiated cancel recording `model_cancelled`, real spawned read-before-write hook over `transcript_path` with `hook_context` republish, transcript completeness + offset increments); `git diff --stat -- agent-core` stays empty. |
| `phase-04.6-agent-runtime-e2e_SPEC.md` | Baseline completed; expansion in progress | Test-only, additive: `packages/agent-runtime/e2e/` live e2e module over the configured `codex_coding_plan` entry (real SSE, real model). The baseline covers agent-loop fundamentals, background supervisor + agent tools, interrupt/wake/steer, and engine-direct `AgentEvent`/SSE ordering. The expanded E2E-12..60 matrix now adds large-scale tool pressure, hooks/notifications, subagent fanout, cancellation timing, recursive cancellation, and isolation targets; six expanded shards are implemented and observed: `batch-policy.e2e.ts` (parallel batch with overlapping execution windows + `tool_use`-ordered results, and `isBatchExecutionForbidden` whole-batch rejection with solo recovery), `subagent-fanout.e2e.ts` (parallel `run_subagent` launches, child `max_turns` failure settlement, nested notification ownership, and `cancel_background_session` cancelling one child while a sibling completes), `hooks-notifications.e2e.ts` (real spawned hook denying by `transcript_path` with the denied call provably never executing plus `hook_context` republish, and `updatedInput` rewrite reaching `execute()` plus `metadata.hook_warnings` on the transcript), `cancellation.e2e.ts` (zero-spend pre-turn abort, mid-SSE abort classified `cancelled` not `provider_error`, first-reason-wins double interrupt, settled-real-result salvage accepted by a live restart, interrupt waking auto-wait into the disposal cascade, and unknown-session + post-delivery cancel recovery), `recursive-cancel.e2e.ts` (parent-interrupt and model-initiated branch cancellation over live grandchildren, child failure and child submission-guard one level down, a settled-undelivered grandchild keeping its natural `completed` close, a three-deep `caller_disposed` unwind by ownership, and sibling-branch isolation), and `cancellation-isolation.e2e.ts` (two live mains with independent cascades and no cross-run id leakage, three-way disposal fanout, and repeated spawn/cancel cycles leaving `[]` session lists). E2E-48/55 (register-after-dispose latch) were found not live-constructible - `run_subagent.execute()` starts and registers the child in one synchronous block - and are recorded unit-only in the matrix. The credential probe runs through the runtime's own `loadLlmClientRegistry`; no `src/` changes in any package; Rust `agent-core/` remains live and unchanged. | Baseline + observed expansion live on this machine (`gpt-5.5` medium): pre-trilogy whole suite 7 files, 19/19 green (~134s); per-shard: `batch-policy` 2/2 (14.2s), `subagent-fanout` 4/4 (46.0s), `hooks-notifications` 2/2 (13.3s), `cancellation` 7/7 (~29s), `recursive-cancel` 7/7 (142.9s full pass), `cancellation-isolation` 3/3 (34.3s); with the trilogy landed the directory holds 10 files / 36 tests and absent credentials clean-skip all 36 in ~2s. Current local checks: `pnpm run check` (282 unit tests), `git diff --stat -- agent-core` empty, and `git diff --check` clean. Observed expanded rows: E2E-17, E2E-20, E2E-23..26, E2E-35..38, E2E-41..47, E2E-49..54, E2E-56..60 (E2E-48/55 unit-only by construction); remaining expanded rows are still target work before the expansion is complete. |
| `phase-04.7-run-audit-log_SPEC.md` | Completed | Additive run audit log: the `@eos/agent-runtime` transcript writer grows into a per-run `RunLog` writing `events.jsonl` (run/turn/tool lifecycle with per-turn `UsageSnapshot` + `cache_hit_rate`), `transcript.jsonl` (the Phase 04.5 transcript lines, filename and schema unchanged; `transcript_path`, hooks, and `read_agent_run_transcript` untouched), and a one-line `result.jsonl` rollup (accumulated `outcome.usage`, run-scope cache-hit rate, identity + timing) under `<dataDir>/runs/<run_id>/` with one shared seq across the three files; plus the `@eos/llm-client` owned change normalizing openai-responses `input_tokens` to net-of-cache so `UsageSnapshot` has one documented semantic and exported `cacheHitRate` is the one formula; no `@eos/engine` changes; Rust `agent-core/` remains live and unchanged. | `pnpm run check` in `eos-agent-core/` green (typecheck, lint, 27 unit files / 294 tests) including the Phase 04.7 §7 suite; focused live `pnpm exec vitest run --config vitest.e2e.config.ts packages/agent-runtime/e2e/agent-loop.e2e.ts` green (4 tests) with the per-turn cache-hit audit assertion; visible full-suite live flakes in E2E-47 and subagent-fanout cancel both passed on focused retry; `git diff --check` clean; `git diff --stat -- agent-core` empty. |
| `phase-04.8-advisory-pass_SPEC.md` | Proposed | Additive advisory enforcement over the current `ask_advisor` mechanism: `ToolDefinition` gains `isAdvisoryRequired` plus non-empty `advisorPrompt`, only `submit_main_outcome` / `submit_planner_outcome` / `submit_worker_outcome` require it, `ask_advisor` receives exact two-message advisor prompts `[caller transcript, advisorPrompt + target tool JSON]`, a universal `require_advisory_pass` `PreToolUse` command hook gates advisory-required tools by scanning the run transcript for the latest exact advisor `pass`, and `submit_main_agent_outcome.ts` is renamed to `submit_main_outcome.ts`; Rust `agent-core/` remains live and unchanged. | Planned: `pnpm run check` in `eos-agent-core/` including tool definition validation, advisory metadata inventory, advisor initial-message exactness, profile selection validation, hook transcript scanner case tables (missing/wrong-tool/wrong-payload/malformed/fail/latest-pass), and focused deterministic e2e proving an exact advisor pass allows the protected terminal call. |
| `phase-04.9-notification-trigger-engine_SPEC.md` | Proposed | Notification trigger system, since extracted into `@eos/notifications` (`packages/notifications/`): the `NotificationInbox`, the `LoopObserver` port (`turnCompleted` awaited after every committed assistant turn including batches, `idleStarted`/`idleEnded` bracketing auto-wait; no clocks or config in `@eos/engine`), the notification-rule schemas (`TriggerRuleEntrySchema` over the package-owned `CommandScriptSchema`, loaded from `.eos-agents/notification_rules.json` with optional `agent_name`/`agent_kind` matchers), and the per-run `NotificationTriggerEngine` (`TurnCompleted`, `IdleParked` + `timeout_ms`; scripts answer `{ notification?: string }`; publishes `{type:"reminder", source}` so an idle-timeout publish wakes the park through `waitForNext`; one-shot timer per park re-armed on re-park, generation guard against wake races, script failures logged and dropped). `@eos/tool` keeps the spawn-backed `runTriggerCommand`; the runtime loads rules in `notification-rules-config.ts` and wires the engine per run. Three reference scripts under `.eos-agents/notification-rules/` are registered in the repo baseline `notification_rules.json` (budget ladder at 50 and 80) for every agent run; notification-only - scripts inform, the model acts; Rust `agent-core/` remains live and unchanged. | `pnpm run check` in `eos-agent-core/` including engine observer placement/abort-path tests, trigger-engine unit tables (rule matching, skip-on-empty, failure swallowing, timer arm/clear, generation guard, re-arm on re-park), and the spawn-backed runner table (skip/exit/JSON/schema/timeout mapping); live e2e over the registered baseline in `notification-triggers.e2e.ts` (spin rescue completing instead of `failed: max_turns`, idle-timeout wake plus wake-first silence, 50/80 budget ladder), with `auto-wait.e2e.ts` rebased on that baseline. |
| `phase-04.10-text-termination-mode_SPEC.md` | Completed | Profile-driven text termination: `terminal_tool` becomes optional in agent profiles (`planner`/`worker` still require it; absence is the switch, first intended for subagents), the engine gains `terminationMode` (`"terminal_tool"` default / `"text"`) with a gated bare-text exit — no pending steers and no open background sessions, the same set the `no-open-background-sessions` submit hook denies on — finishing `completed` with `submission = assistantText(final_message)` and skipping `turnCompleted` on the finishing turn; `TriggerPayload.terminal_tool` widens to `string \| null` with the baseline remind/budget rule scripts branching on `null`; `run_subagent` settlement summaries pass string submissions through verbatim; terminal-tool profiles behave byte-for-byte as today; Rust `agent-core/` remains live and unchanged. | `pnpm run check` in `eos-agent-core/` green (typecheck, lint, 39 unit files / 534 tests) including the §13 unit checklist (engine text-exit rows U1-U9 over `MockLlmClient` + scripted sessions with the pre-existing loop suite green unmodified, profile loader/registry kind gating U10-U12, runtime wiring + verbatim settlement summary U13-U14, trigger payload `null` JSON round-trip U15); baseline rule scripts smoke-checked over piped payloads (`terminal_tool: null` keeps `remind-terminal-submission.cjs` silent and switches `budget-reminder.cjs` to the plain-text wording, non-null wording byte-identical); live `pnpm exec vitest run --config vitest.e2e.config.ts packages/agent-runtime/e2e/text-termination.e2e.ts` green on this machine (T1-T4: text completion without reminders, settlement to parent, park-then-finish, budget wording; 4/4 in 34.6s) and clean-skip without credentials (4 skipped in ~0.4s); `git diff --check` clean; `git diff --stat -- agent-core` stays empty. |
| `phase-05-workflow-orchestration_SPEC.md` | Implemented (with Phase 05.1 amendments) | Additive `@eos/workflow` package (DB-authoritative workflow -> iteration -> attempt -> plan/work-item aggregate per the companion `docs/plans/workflow_context_projection_SPEC.md` with §3 recorded amendments; depth-parametric markdown brief/spec renderers behind two combinators with revision-stamped projections and prior iterations collapsed to status + reference, virtual projection only; lifecycle orchestration functions + per-workflow serial reconcile queue; scheduler consumes planner/worker submissions from `outcome.submission` via a narrow `AgentLaunchPort` - launch-after-commit from claimed queue rows, settlements without valid submissions synthesized as failures) plus first real `@eos/db` content (`better-sqlite3` + `Kysely` workflow schema, `WorkflowStore` transactions/claims/`loadAggregate`), `@eos/contracts` workflow IDs/status/payload schemas, `@eos/tool` owned changes (workflow family `delegate_workflow`/`read_workflow_context`/`query_workflow_context` over bound functions with the one-open-workflow guard, byte-offset paged reads with revision pinning, deduplicated entity-local-field search, per-kind planner/worker submission payload schemas filling the Phase 04 reserved seam, `cancel_background_session` type union gains `"workflow"`), and `@eos/agent-runtime` wiring (optional `workflowDb`, launch-port adapter over `startRun` with fresh abort roots, per-run `workflowTools`, one supervisor session per delegated workflow with disposal cascade); no `@eos/engine` changes; Rust `agent-core/` remains live and unchanged. | Implemented as the combined Phase 05 + 05.1 effort with the §3 amendment rows of `phase-05.1-workflow-context-redesign_SPEC.md` applied (per-field projection instead of brief/spec renderers, bound submissions instead of settlement consumption, `delegate_workflow` alone, no cell/queue registries); verified by `pnpm run check` in `eos-agent-core/` (engine-free `packages/workflow` lifecycle/guard/context/mirror suites over a scripted `AgentLaunchPort` on `:memory:`, tool family + per-kind submission suites, and the `MockLlmClient` runtime end-to-end in `packages/agent-runtime/tests/workflow-runtime.test.ts`); `git diff --stat -- agent-core` stays empty. |
| `phase-05.1-workflow-context-redesign_SPEC.md` | Implemented | Amendment to Phase 05 (landed as one combined effort, this spec winning where they conflict): replaces the workflow context model with planner-declared iteration focus over append-only `(iteration_focus, deferred_goal)` declarations on plans (`plan_spec` deleted; immutable `goal`, focus views, `is_consistent_with_iteration_focus`, and drifted-attempt archive views all derived in `loadWorkflowTree`; refocus supersedes in place under one attempt budget), names two workflow read shapes (`loadWorkflowTree` for the complete UI/render graph and `loadWorkflowContext` for the context path universe), replaces composed `spec.md`/`brief.md` projections with a per-field file path universe (one fact one path, verbatim field files with status as DTO/listing metadata rather than content, latest-render overwrite reads, directory tree listings as the overview, derived `archived/` sections labeled by iteration/attempt id and excluded from search by default) persisted as a post-commit disk mirror under `.eos-agents/workflow/context/workflow_<id>/` (DB stays authoritative; atomic writes; pruned on relocation; write failures non-fatal; tools never read the mirror), and replaces the fixed launch-context policy with `workflow_context` snapshots plus a current-launch locator through one composer seam (built-in default policy or per-profile `workflow_context_script` paths under `.eos-agents/workflow/scripts/`, with helper files such as `variable_reference_map.cjs`, hook-parity JSON stdin/stdout emitting `initial_messages` in the real runtime `Message` shape; compose failures synthesize failed settlements through the Phase 05 §2.7 path; script inputs have no envelope metadata fields); the workflow tool family ships `delegate_workflow` alone with subagent-style supervisor registration (cancel rides `cancel_background_session` type `"workflow"`; read/query tools deferred to a later discussion), and the planner/worker submission tools validate and mutate workflow entity state in-run through an entity-bound `SubmissionBinding` on the launch port (settlements reduce to death synthesis; attempt failure cancels sibling work items); Phase 05's scheduler/reconcile/session machinery is otherwise unchanged; Rust `agent-core/` remains live and unchanged. | Implemented together with Phase 05 and verified under `pnpm run check`, with the Phase 05.1 §16 suite replacing the Phase 05 §14 cases it amends (`loadWorkflowTree` goal-chain/focus/consistent_to_iteration_focus/archive views, `loadWorkflowContext` path universe, per-field projection and listing case tables, flattened planner payload plus first-declaration enforcement, keep/refocus/supersession/budget, composer policy and real spawned `workflow/scripts/planner.cjs` + `worker.cjs` fixtures loading `variable_reference_map.cjs` and proving `work_item_description`/`work_item_spec`, profile-script validation and failure synthesis, disk-mirror byte-equality + relocation prune + non-fatal write failure, package-side resolver/listing over the new path universe); `git diff --stat -- agent-core` stays empty. |
| `phase-05.2-workflow-outcome-context-rendering_SPEC.md` | Implemented | Amendment to the Phase 05.1 context renderer: keeps `Plan` as DB/launch/submission state but removes it as a rendered context entity (`ContextEntityKind` drops `"plan"`, `plan/context.ts` deleted), moves planner summary to attempt-owned `plan_summary.md` (attempt listing rows take its first line), adds attempt-level `outcome.md` derived from work-item statuses/summaries in planner order (`(no work items)` for planner death/compose failure), defines iteration outcome as the closing attempt outcome (`Success`/final `Failed` only), defines workflow outcome as all closed iteration outcomes in sequence order with a non-business `workflow cancelled` marker on cancellation, removes plan `context_path` from workflow-context script DTOs while keeping plan metadata, and records logical file/folder first-appearance times for deterministic context and mirror tests; Rust `agent-core/` remains live and unchanged. | Implemented and verified by the §10 suite in `packages/workflow/tests/context.test.ts` (plan flattening, attempt outcome timing/order, retry vs final-failure outcome creation, planner-death `(no work items)`, multi-iteration ledger, cancellation marker, refocus archive keeping flattened attempt files), `packages/workflow/tests/mirror.test.ts` (byte equality plus no `plan_<id>/` directories on disk), and `packages/contracts/tests/workflow.test.ts` (plan DTO keeps metadata, rejects `context_path`); `pnpm run typecheck`, `pnpm run lint`, and `pnpm run test` (38 files, 424 tests) green; `git diff --check -- docs/plans/agent-core-rust-to-typescript-migration eos-agent-core` clean. |
| `phase-06-question-escalation_SPEC.md` | Proposed | Additive question-escalation family over the combined Phase 05+05.1 surface: blocking `ask_question` / `answer_question` riding the delegation tree's two edges (user -> main through an injected `UserQuestionPort` with no answer tool, Claude Code AskUserQuestion parity; delegating main/worker -> planner through a `QuestionBinding` on the launch options beside `SubmissionBinding`), tool exposure conditional on a live answer path (headless main and binding-less planners assemble no `ask_question`), kind-validated assignment at startup (ask: main/planner; answer: main/worker; advisor/subagent neither), no addressee on either tool (route = binding/port; `answer_question` resolves a typed `question_id` against the service registry with the recorded delegating run as the only authorized answerer), append-only `questions` rows keyed to the asking plan with terminal-guarded single resolution and consistency-derived archiving on refocus, `question_<id>/question.md`+`answer.md` in the §9 context universe and disk mirror, answered/declined clarifications surfacing in retry-planner `workflow_context` with an unanswered-question escalation directive, a closed outcome union (answered/declined/expired/cancelled) with opt-in `workflowQuestionTimeoutMs` (no default clock; wedge protection rides the cancel/dispose cascades and death synthesis), the worker fail-upward protocol (cancel the child workflow, then `is_pass: false` quoting the question verbatim), a self-contained `question_pending` notification (publisher-side; inbox unchanged), the engine drain site aggregating each drain into one user message, and `ask_question` batch-solo via `isBatchExecutionForbidden`; Rust `agent-core/` remains live and unchanged. | Planned: `pnpm run check` in `eos-agent-core/` including the Phase 06 §14 suite (contract tables, terminal-guarded resolution races on `:memory:`, engine-free ask/answer round-trip with decline/timeout/cancel/death tables over a scripted launch port, retry-context clarifications + refocus archiving, question-file projection and mirror byte-equality, exposure + kind-validation startup failures, batch-solo recovery, drain aggregation ordering, one MockLlm chained-escalation end-to-end with verbatim forwarding to a scripted `UserQuestionPort`, and the worker fail-upward guard ordering); `git diff --stat -- agent-core` stays empty. |

## Knowledge Base

| Note | Status | Purpose |
| --- | --- | --- |
| `knowledge/claude-code-tech-stack.md` | Observed | Record local Claude Code source-stack observations and migration takeaways for eos-agent-core. |
| `knowledge/ask-user-question-tool.md` | Observed | Record Claude Code's AskUserQuestion permission-layer question/answer design (no answer tool, main-thread-only exposure, headless guard) and migration takeaways. |
| `knowledge/inter-agent-messaging-protocol.md` | Observed | Record Claude Code's SendMessage/mailbox/team-tool protocol surface (routing tree, poll-based delivery, per-role tool matrix) and migration takeaways. |
| `workflow-background-supervisor-side-note.md` | Observed | Clarify that `@eos/workflow` does not depend on `@eos/background`; the runtime/tool adapter registers the delegated workflow terminal handle with the parent run's supervisor. |

## Current Boundary

`agent-core/` remains the live Rust implementation until a later phase spec
replaces or retires each Rust-owned surface with verified TypeScript equivalents
under `eos-agent-core/`. The expected migration specs should preserve explicit
ownership for runtime entry, workflow state, engine/query loop, tool framework,
provider clients, sandbox host API, config, audit, skills, and plugin catalog.

## Tracker Discipline

Every migration phase added under this directory must update this index with:

- the phase spec path,
- the phase status,
- the live implementation boundary it changes,
- and the verification command or artifact that proves the phase.
