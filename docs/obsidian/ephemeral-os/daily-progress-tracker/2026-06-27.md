---
title: 2026-06-27
date: 2026-06-27
tags:
  - ephemeral-os
  - daily-progress-tracker
status: draft
---

# 2026-06-27

## Task Tracker

### Main Tasks

- [x] Re-organize `prd.config`, and the `/eos` path under it #task/main
- [x] Optimize observability trace, log, and perf #task/main
- [x] Observability rework: collapse to one dependency-light NDJSON leaf crate (drop SQLite) — spec: `docs/observability-rework/README.md` #task/main
- [x] Justify naming convention: `host-compatible` vs `isolated` #task/main
- [x] Justify naming convention:`runtime` vs `manager` #task/main
- [x] Create doc for `cli`, `gateway`, `runtime`, and `manager` connection #task/main
- [x] Implement `network_profile` arg refactor on `create_workspace_session` (rename `--profile`/`profile` → `--network-profile`/`network_profile`) — spec: `docs/network-profile-refactor/README.md`; recommended scope A (wire-only) #task/main

### Subtasks

#### Re-organize `prd.config`, and the `/eos` path under it

- [x] Remove unused/default `prd.yml` config fields #task/subtask
- [x] Revisit and simplify `/eos` path layout #task/subtask

#### Observability Rework

- [x] Write specs: main + layerstack + CLI (`docs/observability-rework/{README,layerstack-observability,cli-observability}.md`) #task/subtask
- [x] Phase 1 — crate rework: one-record `span` (`ts`+`dur_ms`), `Sink` (`O_APPEND`, one write/line), `Observer`/`SpanGuard`/`AsyncSpan`/`TraceContext`, `Reader` + views, `paths.rs` retarget, move `collect/{cgroup,disk}` #task/subtask
- [x] Phase 2 — daemon swap: build `Observer`; `NoopObserver`→`ObsExecutionObserver`; `collect()`→`obs.sample`; serve snapshot from live `observability_snapshot()`; generalize `get_observability` op + CLI #task/subtask
- [x] Phase 3 — replace ~56 in-sandbox `timing::` sites with span guards/events; thread trace id = `Request.request_id` (thread-local at dispatch + capture into watcher/finalize so Case A tail correlates) #task/subtask
- [x] Phase 4 — layerstack events: `lease.acquired`/`layerstack.publish`/`publish_rejected`/`lease.released` #task/subtask
- [x] Phase 5 — removal checklist: 5 in-sandbox `timing.rs`, `runtime_timing_env`, `rusqlite`/`store/**` → scoped greps to zero; repoint boundary test #task/subtask
- [x] Layerstack observability: `collect/layerstack.rs` reader (manifest + per-layer bytes, sidecar cache) + leased/booked-by/needed in snapshot + cgroup `io.stat` #task/subtask
- [x] CLI observability family: `snapshot`/`trace`/`events`/`cgroup`/`layerstack` subcommands (`--sandbox-id`) + help catalog — spec: `docs/observability-rework/cli-observability.md` (raw view dropped per post-review spec) #task/subtask
- [x] Phase B (follow-up): thread `trace`/`parent` through `NamespaceRunnerRequest` for the cross-process tree (forked namespace-process) — commit `cdd893e25` #task/subtask

#### Network Profile Refactor

- [x] Refactor misleading `profile` vocabulary in `sandbox-runtime-workspace/src` → manager/mounted-workspace naming — spec: `docs/workspace-session-rename/README.md`; approved Tier A + B #task/subtask
- [x] Write rename migration spec (resolved: both tiers, `MountedWorkspace`, `open`/`close`, `isolated_network_setup`, migrate obs DB schema) #task/subtask
- [x] Types: module `profile/`→`session/`, `isolated_setup/`→`isolated_network_setup/`, `WorkspaceProfileManager`→`WorkspaceManager`, `WorkspaceProfileHandle`→`MountedWorkspace`, `WorkspaceProfileFds`→`HolderNsFds`, `WorkspaceProfileError`→`WorkspaceManagerError` #task/subtask
- [x] Verbs: `enter_with_profile`→`open`, `exit`→`close`; `active_profile_id`→`active_session_id` + locals #task/subtask
- [x] Tier B field: typed `profile`→`network` (handle/DTO); string surfaces→`network_profile` (coordinate with this item's Option C — wire/DTO/handle together) #task/subtask
- [x] Bundled cleanups: drop dead `WorkspaceProfileError::kind()`, drop duplicate `record_create_phase_ms`, collapse `capture_changes` double-lookup #task/subtask
- [x] Obs DB migration: append schema V9 `RENAME COLUMN profile TO network_profile` + record/row/read/write/JSON + `manager.json` key; bump `schema.rs` test `checksums.len()` 8→9 #task/subtask
- [x] Update cross-crate call sites: `operation`/`daemon`/`observability` + workspace `tests/unit/{service,model}.rs` #task/subtask
- [x] Verify: `cargo fmt` / `build` / `clippy --all-targets` / `cargo test` (incl. `-p sandbox-observability`) + v8→v9 DB upgrade check #task/subtask

## Artifact

### Create doc for `cli`, `gateway`, `runtime`, and `manager` connection

**Artifact:** [[docs/cli-gateway-manager-runtime|CLI Gateway Manager Runtime]]

**Session:** Created a user-facing Obsidian note for operators that explains how `bin/start-sandbox-docker-gateway`, `bin/sandbox-cli`, `sandbox-gateway serve`, `SandboxManagerRouter`, Docker setup, `sandbox-daemon serve`, and `sandbox-runtime operations` fit together.

**Included:**

- A system map split into User Tools, Gateway Layer, Manager Layer, Docker Runtime Setup, and Sandbox Runtime.
- Smaller diagrams for gateway startup, CLI manager requests, sandbox creation/runtime setup, and runtime command forwarding.
- Minimal operator commands and path-only implementation references.

### Justify naming convention: `host-compatible` vs `isolated`

**Commit:** `10b70de64` — Rename WorkspaceProfile to NetworkProfile and HostCompatible to Shared

**Verdict:** The two profiles differ on exactly one axis — the network boundary.
Both keep a private overlay plus mount/pid/user namespace isolation; only the net
namespace differs. The old names were wrong on two counts:

- `WorkspaceProfile` / `HostCompatible` overclaimed a general "isolation" axis and
  read as "this is the host / localhost", when host-compatible is actually
  isolated in every dimension except network.
- The variants were not even lexically parallel: `host_compatible` framed on
  network sharing, `isolated` framed on general isolation.

**Resolution (shipped in the commit above):**

- Renamed the type `WorkspaceProfile` → `NetworkProfile`, scoping the whole
  selector to the network axis. This fixes the overclaim for free —
  `NetworkProfile::Isolated` now unambiguously means network-isolated.
- Renamed the variant `HostCompatible` → `Shared` (paired with `Isolated`) and
  changed the wire/CLI value `host_compatible` → `shared`, so AI/CLI readers stop
  inferring "the host". `Isolated` unchanged.
- Reworded the `create_workspace_session` / `exec_command` descriptions to state
  the workspace shares the host network namespace while staying isolated in
  mount/pid/user — explicitly "it is not the host".
- Confirmed network (profile) is orthogonal to lifecycle (one-shot vs session);
  the one-shot path simply pins `Shared`. Did not leak one-shot/session into the
  profile vocabulary.
- Removed the transitional `host_compatible` parse alias as unused legacy.

**Verification:** `cargo build` (no warnings), `cargo clippy --all-targets`, and
`cargo test` all clean for the changed crates. Net diff 120 +/127 −.

### Refactor `profile` vocabulary in `sandbox-runtime-workspace` → session/manager

**Commit:** `440f8a925` — Rename workspace profile vocabulary to session/network and collapse handle redundancy

**Session:** Implemented the full `docs/workspace-session-rename` spec across
`sandbox-runtime-workspace`, `-operation`, `-daemon`, and `-observability`, then
folded in three extra dedup refactors. No behavior change; all sites landed in one
commit so the workspace stays green.

**Tier A (vocabulary):**

- `profile/` → `session/` (`handle.rs` → `state.rs`); `isolated_setup/` →
  `isolated_network_setup/`.
- `WorkspaceProfile{Manager,Handle,Fds,Error}` → `WorkspaceManager` /
  `MountedWorkspace` / `HolderNsFds` / `WorkspaceManagerError`.
- `enter_with_profile`/`exit` → `open`/`close`; `active_profile_id` →
  `active_session_id`; `//!` docs reworded "isolation profiles" → "network modes".

**Tier B (field):** typed `.profile: NetworkProfile` → `.network`; serialized
string surfaces (SQL column, `Option<String>` record/row fields, JSON output key,
`manager.json` key) → `network_profile`. Left `NetworkProfile`/`Shared`/`Isolated`/
`as_str()` untouched.

**Observability DB:** append-only migration **V9** `RENAME COLUMN profile TO
network_profile` (SQLite preserves rows; V2 SQL left byte-for-byte so its checksum
stays locked); bumped the `schema.rs` tests `checksums.len()` / `migration_count`
8 → 9.

**Bundled + follow-up cleanups:**

- Dropped dead `WorkspaceManagerError::kind()` and the duplicate
  `record_create_phase_ms` (4 call sites repointed to `super::record_phase_ms`).
- Collapsed the `capture_changes` and `destroy_workspace` double-lookups —
  `active_session_id` removed since `close()` already returns `NotOpen`.
- Replaced the stored, hand-synced `WorkspaceHandle.base_revision` with a derived
  `base_revision()`. Safe because the publish-refresh path is dead and capture
  echoes the current revision, so the field never diverged from `snapshot`.
- Collapsed `MountedWorkspace`'s five scattered manifest/lease fields into one held
  `LayerStackSnapshotRef`.

**Verification:** `cargo fmt`, `cargo build --all-targets` (no warnings), `cargo
clippy --all-targets` clean; tests pass for the four crates; verified a v8→v9 DB
upgrade returns prior `network_profile` values. Grep gate: none of
`WorkspaceProfile`, `crate::profile::`, `isolated_setup`, `enter_with_profile`,
`active_profile_id`, or `record_create_phase_ms` remain. Net 42 files, +312 / -374.

### Implement layerstack observability (lease-state cleanup + reader/views)

**Commits:** `6823d843e..f68dadd0d` (11) — spec: `docs/observability-rework/layerstack-impl.md`

**Session:** Implemented the layerstack slice of the observability rework — the
first reader-first vertical. Part A makes `leases` the single source of truth so
Part B's per-layer facts are honest by construction; Part B adds the in-memory
lease observation, a pure disk-bytes leaf reader, the daemon merge into
`layerstack`/`cgroup`/`snapshot` views, and the `sandbox-cli observability`
family. Landed in small per-step commits, workspace green at each.

**Part A — lease-state cleanup (`layerstack/src`):**

- Deleted the denormalized `refcounts` cache + `increment/decrement_layers`; the
  registry derives the mounted-layer union from `leases` on demand (sole consumer
  `cleanup.rs`). `grep refcounts` in `layerstack/src` → 0.
- Unified `Lease` + `LeasedSnapshot` → one `Lease` (`{lease_id, manifest,
  layer_paths: Vec<PathBuf>}`); version/hash via accessors (dropped the lossy
  `Vec<String>` round-trip, the stored scalars, and `snapshot_from_lease`). The
  no-lease `Snapshot` keeps its scalar fields, so `get_snapshot` stays
  non-breaking.

**Part B — observability:**

- `LayerStack::observe()` (own `layerstack/src/observability.rs` module) →
  `StackObservation` per-layer lease breakdown, one pass over live `leases`,
  base→newest.
- Pure `collect/layerstack.rs` leaf reader in `sandbox-observability` (`std` +
  `serde_json` only, no layerstack dep): manifest + per-layer bytes via a
  `.layer-metadata/<id>.bytes` sidecar (written at publish) with a du-walk
  fallback that repopulates; half-written manifest → skip, never panic. Added the
  obs→layerstack `dependency_guard` boundary test.
- Daemon `get_observability` op merges `observe()` + `sample_layerstack()`:
  **layerstack** inventory (`leased by N ws`, `booked by <ids>` derived at
  render), **snapshot** (live state + stack line), **cgroup** (per-scope series),
  and the **`--workspace`** per-session view (mounts + `shared with`). Coexists
  with the SQLite `get_observability_snapshot` (kept until the main spec's removal
  phase).
- Minimal NDJSON `SampleSink`/`SampleReader`; daemon `collect()` emits the
  periodic `scope:"stack"` sample feeding the layerstack `--window-ms` trend (the
  only NDJSON write).
- New read-only `observability` CLI execution space + family
  (`layerstack`/`cgroup`/`snapshot`); the manager auto-forwards the
  sandbox-scoped op to the daemon (no manager change). Golden help +
  request-mapping tests.

**Deferred (later phases):** `trace`/`events`/`raw` CLI views (the span/trace
half — main-spec phases 1–4); cgroup `io.stat`; the SQLite→NDJSON cutover. The
`needed` concept was intentionally dropped — `booked by` replaces it.

**Verification:** `cargo build` / `clippy --all-targets` / `test` / `fmt` green
workspace-wide at every step; final **388 tests, 0 clippy warnings**, fmt clean.
No new `unwrap_used` / `dbg_macro` / `unsafe`. Grep gates: `refcounts` → 0 in
`layerstack/src`; obs leaf crate carries no runtime/layerstack/daemon dep.

### Implement CLI observability family (five views, drop raw)

**Commit:** `6aee8aeee` — Update CLI observability specs and docs (one follow-up
`events` truncation refactor still in the working tree).

**Session:** Built the post-review `sandbox-cli observability` surface to match
`docs/observability-rework/cli-observability.md` — **five** views (`snapshot`,
`trace`, `events`, `cgroup`, `layerstack`), `--sandbox-id` required on every view,
id-valued flags carrying `-id`, and **no `raw` view**. Finishes the `trace`/`events`
CLI half deferred from the layerstack slice and reshapes the existing views to the
final spec.

**CLI specs (`observability_specs.rs`):**

- Added `TRACE_SPEC` (`--trace-id`, optional, default `last`) and `EVENTS_SPEC`
  (`--name`, `--since-ms`, `--last-n`), transcribed from spec §2.
- Renamed layerstack `--workspace` → `--workspace-id` (arg `workspace` →
  `workspace_id`); reconciled `snapshot`/`cgroup` descriptions to §2.
- Ordered the catalog `[snapshot, trace, events, cgroup, layerstack]` so help
  renders in §3.1 order; dropped the `raw` spec.

**Daemon router (`view.rs` / `service.rs` / `reader.rs`):**

- Removed the `raw` view (`raw_view_response`, `raw_filter`,
  `DaemonObservability::raw_lines`).
- `trace` reads `trace_id`, fault text `--trace-id`, and resolves the `last`
  sentinel to the most-recently-started root trace via a new
  `Reader::latest_root_trace()` (root span = no parent, max `ts - dur_ms`).
- `layerstack` reads `workspace_id`; `events` applies `--last-n` post-fold as a
  newest-N truncation (not a `RawFilter` field).

**Mapping / output:** the request builder stays view-generic — flags forward 1:1
to `get_observability` params (`trace_id`/`last_n`/`workspace_id`); each view
returns lightly-structured `{ "view": … }` JSON dumped as-is (human renderers
deferred — trace waterfall first when they land).

**Review pass:** confirmed the raw-view removal is complete tree-wide (no
production leftovers; protocol/dispatch/lib re-exports clean). Kept `Reader::raw` —
it has no production callers now but is the public inspection surface over the
private `scan()` primitive, used by test suites in three crates to read the log
back with scan's sort/rotation-merge/skip semantics; removing it would force tests
to reimplement `scan()`. Refactored the `events` `last_n` truncation to a
`saturating_sub` + `drain` idiom (removes an apparent-underflow read).

**Verification:** `cargo fmt` and `cargo clippy --all-targets` clean;
`sandbox-gateway` **48** tests and `sandbox-daemon` **50** tests pass — incl. new
catalog/per-op help golden tests for all five views, request-builder param-mapping
tests, and daemon view-behavior tests for `last_n` truncation and `last`
resolution. One pre-existing, unrelated failure remains:
`sandbox-manager::get_observability_tree_converts_one_daemon_failure_to_one_unavailable_node`
(no dependency on the changed crates; fails at clean `HEAD`).
