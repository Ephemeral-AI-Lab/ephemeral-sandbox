/goal Implement the LayerStack Squash + Live Workspace Remount feature end to end, exactly as designed in docs/obsidian/ephemeral-os/implementation_plan/squash/spec.md, working phase by phase through impl_plan_and_progress_tracker.md in the same folder, finishing only when every box in acceptance_criteria.md is checked.

First read, fully: spec.md (source of truth), the tracker (your working document), acceptance_criteria.md (definition of done), simplicity_review_results.md (rationale — consult before adding any type, file, fallback, or field; the answer is almost always no).

Non-negotiable rules (from the tracker):
1. Experiment-first. Each phase lists "Experiments — must complete BEFORE implementation". Run them on the real machine (bin/start-sandbox-docker-gateway; scratch probes under /tmp or tests/, never src/) and record command + result in the tracker's Experiment log before writing any production code for that phase. Phase 0 is experiments only — kernel/fs preconditions, same-upperdir coexistence (X0.2, go/no-go for the remount half), userxattr resurrection, mount-move semantics, EBUSY via SCM_RIGHTS-parked fd, syncfs benchmark, OVL_MAX_STACK errno, hardlinks, freeze cost, mountinfo observation. If X0.2/X0.3 fail irrecoverably: descope phases 5–7 per tracker rule 4, record it, ship squash commit-only.
2. Phase gates. Before the next phase, complete the Exit review: every box checked, cargo test green for touched crates, clippy --all-targets and fmt clean, spec-drift check done, Progress table and logs updated. Never skip ahead or batch reviews.
3. Spec stays true. If an experiment disproves a claim or an API doesn't fit, update spec.md and the Decision log in the same change — code and spec never diverge.
4. Simplicity is a requirement. No new state if manifest/lease/manager state carries the fact; no retry queues; no fallback unless deleting it breaks correctness; substitution map stays in-memory; S layers get zero sidecars; publish.rs gets a zero diff; runner report is two booleans + detail; result JSON is exactly manifest_version + squashed_blocks + faulty_sessions.
5. Repo law (CLAUDE.md): no test code in src/; no inline comments in production code; work directly on main; touch only what the phase requires; deps via [workspace.dependencies].

Phase order (full checklists live in the tracker): 0 environment ground truth → 1 flatten (pure) → 2 substitution map + acquire_rewritten_lease → 3 squash transaction + commit GC (literally the plan-lease release, returning the removed set) + boot sweep in lease/cleanup.rs + single-syncfs commit → 4 overlay move_mountpoint/strict_unmount → 5 quiesce (cgroup ∪ ns-scan ∪ allowlist; ONE holder mountinfo read per session) → 6 staged-switch runner (masks restored before the first move via pre-opened dirfds; EBUSY = park) → 7 workspace transaction + boot reap in persistence.rs + PDEATHSIG → 8 operation layer (per-session gate subsumes and deletes session_lifecycle_lock; squash op + sweep loop; squash_layerstack registered cli: None) → 9 manager checkpoint_squash under the existing "management" family via router/forward.rs → 10 live Docker e2e G1–G3 + E1–E10, enablement, sign-off.

Never violate: storage commit never depends on remount; never mutate or delete a layer any live lease references; replacement lease acquired before the old is released; old lease releases only after strict unmount or namespace death — EBUSY parks BOTH leases until destroy; PONR = first MS_MOVE success: before it, always a clean skip; at/after it, verified, park, or faulty only; faulty sessions land in faulty_sessions and are destroyed via the ordinary path; boot is fail-closed reap-then-sweep.

Finish by walking acceptance_criteria.md top to bottom, checking each criterion with its listed verification, then update every tracker table and log.
