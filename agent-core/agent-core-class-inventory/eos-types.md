# Crate `eos-types` — Class Inventory

> Generated type & field reference. Source of truth is the code under
> `agent-core/crates/eos-types/src/`. Declarations are enumerated with ripgrep
> and field/variant/trait-item data is read directly from source; one-line
> purposes come from `///` doc comments (or, where absent, a reviewer
> summary). Module-scope types only — test-only (`#[cfg(test)]`) and fn-local
> helper types are excluded. This generated inventory is distinct from any
> hand-curated architecture memory layer.

**19 types across 4 files.**

The `eos-types` crate owns the small, dependency-light value primitives every other crate in agent-core shares — it is the leaf crate of the workspace dependency DAG, so it consumes no sibling crate and is consumed by all of them. It holds the twelve typed string identifiers (`RequestId`, `TaskId`, `WorkflowId`, `IterationId`, `AttemptId`, `AgentRunId`, `SandboxId`, `ToolUseId`, `InvocationId`, `WorkflowSessionId`, `CommandSessionId`, `SubagentSessionId`), all minted by the internal `define_id!` codegen macro as `#[repr(transparent)]` string newtypes; the `UtcDateTime` UTC-instant wrapper plus the `Clock` trait seam (with `SystemClock` and `TestClock` implementations) for deterministic time injection; the transitional untyped `JsonValue`/`JsonObject` aliases that downstream crates parse into typed shapes at their boundaries; and the single minimal `CoreError` enum covering the two failures these primitives can raise. It deliberately holds no domain state, status enums, SQL, HTTP, or config — those belong to their owning crates. The public surface is re-exported flatly from `lib.rs` so consumers write `use eos_types::{TaskId, UtcDateTime, Clock, JsonObject};`.

## Contents

- **`eos-types/src/error.rs`** — `CoreError`
- **`eos-types/src/ids.rs`** — `RequestId`, `TaskId`, `WorkflowId`, `IterationId`, `AttemptId`, `AgentRunId`, `SandboxId`, `ToolUseId`, `InvocationId`, `WorkflowSessionId`, `CommandSessionId`, `SubagentSessionId`
- **`eos-types/src/json.rs`** — `JsonValue`, `JsonObject`
- **`eos-types/src/time.rs`** — `UtcDateTime`, `Clock`, `SystemClock`, `TestClock`

---

## `eos-types/src/error.rs`

#### `CoreError`  ·  _enum_  ·  derives: `Debug, thiserror::Error`  ·  #[non_exhaustive]  ·  [L12]

The single cross-crate error enum for the value primitives in this crate; deliberately tiny, covering only an empty id string, a malformed RFC 3339 timestamp, and a flattened downstream store-operation failure.

**Variants**:
- `EmptyId { kind: &'static str }` — an identifier `FromStr` rejected an empty string; `kind` is the newtype name (e.g. `"TaskId"`) so callers can report which id failed.
- `Timestamp(time::error::Parse)` — an RFC 3339 timestamp string failed to parse into a `UtcDateTime` (`#[from]`).
- `Store(String)` — a persistence/store operation failed; the concrete downstream error is flattened to its `Display` string here.

**Trait impls**: `Error, Display, From<time::error::Parse>`

---

## `eos-types/src/ids.rs`

All twelve identifiers are `#[repr(transparent)]` `String` newtypes generated by the `pub(crate)` `define_id!` macro (declaration line = the `define_id!` invocation). Each derives `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema` with `#[serde(transparent)]`, and the macro hand-writes `Display`, `FromStr`, `TryFrom<String>`, and `TryFrom<&str>` (all with `Error = CoreError`, enforcing the non-empty invariant). The `mint` variant adds `new_v4`; the lone `no_mint` id (`ToolUseId`) omits it.

#### `RequestId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L94]

Identifier for a request row; minted as `str(uuid.uuid4())` in `runtime/entry.py`, reproduced exactly by `new_v4`.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `TaskId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L100]

Identifier for a persisted Task row; opaque string, the root mint being `root-<uuid4().hex[:16]>` (produced by the owning crate).

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `WorkflowId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L106]

Identifier for a delegated Workflow row.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `IterationId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L111]

Identifier for an Iteration row.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `AttemptId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L116]

Identifier for an Attempt row.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `AgentRunId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L121]

Identifier for an agent run (engine agent factory).

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `SandboxId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L126]

Identifier for a provisioned sandbox.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `ToolUseId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L131]

Identifier for a single tool use; model-assigned by the provider stream, so it has no local `new_v4` mint (`no_mint`).

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (2)</summary>

`as_str`, `into_inner`

</details>

#### `InvocationId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L137]

Identifier for a sandbox tool-call invocation (tool dispatch).

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `WorkflowSessionId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L142]

Identifier for an engine background workflow-handle session — the agent-facing `wf_<counter>` handle for one delegated workflow (distinct from `WorkflowId`, which it tracks).

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `CommandSessionId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L151]

Identifier for a sandbox command-session tool handle.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

#### `SubagentSessionId`  ·  _struct_  ·  derives: `Debug, Clone, PartialEq, Eq, Hash, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L156]

Identifier for an engine subagent supervisor session; the runtime mint is `subagent_<counter>` (`new_v4` is the generic helper, not that shape).

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `String` |  |

**Trait impls**: `Display, FromStr, TryFrom<String>, TryFrom<&str>`

<details><summary>Methods (3)</summary>

`as_str`, `into_inner`, `new_v4`

</details>

---

## `eos-types/src/json.rs`

#### `JsonValue`  ·  _type alias_  ·  = `serde_json::Value`  ·  [L10]

Untyped JSON value, mirroring the Python `JsonValue = Any` in `audit/base.py`.

#### `JsonObject`  ·  _type alias_  ·  = `serde_json::Map<String, serde_json::Value>`  ·  [L14]

Untyped JSON object map used for transitional metadata; the owned transitional-metadata contract from spec-conventions §5.

---

## `eos-types/src/time.rs`

#### `UtcDateTime`  ·  _struct_  ·  derives: `Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord, Serialize, Deserialize, JsonSchema`  ·  #[repr(transparent)]  ·  [L29]

A UTC instant; wraps `OffsetDateTime`, guaranteeing the offset is always UTC so cross-provider/DB timestamps are substitutable.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `.0` | `OffsetDateTime` | `#[serde(serialize_with = "...rfc3339::serialize", deserialize_with = "deserialize_rfc3339_utc")]`, `#[schemars(schema_with = "rfc3339_schema")]` |

<details><summary>Methods (5)</summary>

`now`, `from_offset`, `to_rfc3339`, `parse_rfc3339`, `into_inner`

</details>

#### `Clock`  ·  _trait_  ·  bases: `Send + Sync`  ·  [L100]

Source of the current wall-clock instant; the DIP seam injected instead of reading the global clock so tests are deterministic.

**Trait items**:
- `fn now(&self) -> UtcDateTime;`

#### `SystemClock`  ·  _struct_  ·  derives: `Debug, Clone, Copy, Default`  ·  [L107]

Production clock backed by the system wall clock.

_Unit struct — no fields._

**Trait impls**: `Clock`

#### `TestClock`  ·  _struct_  ·  derives: `Debug`  ·  [L118]

Test clock with a settable instant for deterministic tests; the instant lives behind an `RwLock` because reads dominate.

**Fields**

| name | type | vis / attrs |
|------|------|-------------|
| `instant` | `RwLock<UtcDateTime>` |  |

**Trait impls**: `Clock`

<details><summary>Methods (2)</summary>

`new`, `set`

</details>
